# Stub config — local-only registry and aliases live in config.toml.example.
# Copy it to a private location (e.g. .cargo/config.toml.local) and `include`
# it from here if you want the local registry alias.
#
# CI runners should keep this file empty so Cargo never tries to reach
# the loopback mirror declared in config.toml.example.

[alias]
# xtask alias intentionally absent here — uncomment in config.toml.example
# once you've copied it locally.
